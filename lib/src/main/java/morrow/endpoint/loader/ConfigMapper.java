package morrow.endpoint.loader;

import java.util.List;

public class ConfigMapper {
    public List<morrow.endpoint.loader.EndpointDescriptor> map(EndpointConfig endpointConfig) {
        return null;
    }
}
